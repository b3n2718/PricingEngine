# pyproject.toml
[build-system]
requires = ["setuptools", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "mc_engine"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["mc_engine*"]